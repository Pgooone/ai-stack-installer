# 一些指令公共版

## 🔐 Supabase 数据库密码

`Openssl997@@`

---

## 🤖 Claude Code 启动命令

以下是常用的 Claude Code CLI 启动与更新命令：

```bash
claude --dangerously-skip-permissions
claude --verbose --dangerously-skip-permissions --effort max
claude --verbose --dangerously-skip-permissions --effort xhigh
claude --verbose --dangerously-skip-permissions --effort xhigh -c
claude --resume --verbose --dangerously-skip-permissions --effort max
claude update
claude -c --verbose --dangerously-skip-permissions
```

---

## 👁️ Claude Code Monitor 监控功能（v2.1.98+）

Monitor 是 Claude Code 的事件驱动后台监控工具，能让 Agent 挂一个持久的后台脚本，**有事才醒，没事不动**，从 polling 变成 event-driven，大幅节省 token。

### 使用方式

必须在提示词中**明确告诉 Claude Code 使用 Monitor**，否则它会默认用轮询。

```
# 示例 Prompt（中文）
启动我的开发服务器，然后用 Monitor 监控有没有报错。

# 示例 Prompt（英文）
Use the MonitorTool to observe the app logs for errors.

# 部署后监控日志
我刚部署了 API，帮我监控一下日志里有没有报错。
```

Claude 收到指令后会自动生成一个后台监控脚本，例如：

```bash
tail -f /var/log/app.log | grep --line-buffered ERROR
```

### 适用场景

- **日志监控**：盯着应用日志，出现错误自动排查
- **CI/CD 状态**：检查 PR 的 CI 状态，跑完再通知
- **开发服务器**：编译报错自动修复
- **数据库变更**：监控 migration 是否执行成功
- **部署验证**：部署后盯一段时间，确认服务稳定

### Monitor vs /loop vs /schedule 对比

| 特性 | `/loop`（闹钟🔔） | `/schedule`（管家🧹） | Monitor（看门狗🐕） |
| --- | --- | --- | --- |
| **触发方式** | 固定间隔轮询 | 定时计划任务 | 事件驱动，有事才触发 |
| **运行环境** | 本地终端 | Anthropic 云端 | 本地后台进程 |
| **Token 消耗** | 高（无事也跑） | 中（按计划跑） | 低（仅触发时消耗） |
| **适用场景** | 短期、高频巡检 | 周期性维护（如每日同步 PR） | 被动等待异常事件 |

> **选择原则：** 「定时需要做什么」→ `/loop` 或 `/schedule`；「出事了需要处理什么」→ Monitor。三者可以组合使用。
> 

### 注意事项

- 必须在 prompt 中**明确提到 Monitor**，否则 Claude 会用轮询
- 监控脚本粒度很重要：太粗容易误触发，太细容易漏问题
- 目前主要适合**本地开发场景**，远程日志需先拉到本地
- 权限规则与 Bash 工具一致（共用 allow/deny 策略）
- 仅 Claude Code CLI 可用，Bedrock / Vertex AI 暂不支持

---

## 🗑️ Claude Code 原生安装卸载指令

卸载 Claude Code 主程序及相关文件：

```powershell

#跳过初次登录验证
{
  "hasCompletedOnboarding": true,
  "CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC":"1"    #好像是禁用遥测等等问题
}

# 卸载 Claude Code 主程序
Remove-Item -Path "$env:USERPROFILE\.local\bin\claude.exe" -Force
Remove-Item -Path "$env:USERPROFILE\.local\share\claude" -Recurse -Force
# 测试下载链接
curl -fsSL https://storage.googleapis.com -o $null
# powershell 安装
irm https://claude.ai/install.ps1 | iex

#测试Claude更新ping点
storage.googleapis.com

#如果在 PowerShell 中运行命令时出现"无法加载脚本，因为在此系统上禁止运行脚本"错误，请以管理员身份运行 PowerShell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser

# CMD 安装
curl -fsSL https://claude.ai/install.cmd -o install.cmd && install.cmd && del install.cmd
# Winget
winget install Anthropic.ClaudeCode
```

如果还想删除配置文件（这将删除所有设置、允许的工具、MCP 服务器配置和会话历史记录）：

```powershell
# 删除用户设置和状态
Remove-Item -Path "$env:USERPROFILE\.claude" -Recurse -Force
Remove-Item -Path "$env:USERPROFILE\.claude.json" -Force
```

删除项目特定设置（在项目目录中运行）：

```powershell
# 删除项目级配置
Remove-Item -Path ".claude" -Recurse -Force
Remove-Item -Path ".mcp.json" -Force
```

## ⬇️ Claude Code NPM版降级命令

如需回退到指定版本或关闭自动更新，可按以下步骤操作：

```bash
# 卸载当前版本
npm uninstall -g @anthropic-ai/claude-code

# 安装指定版本
npm install -g @anthropic-ai/claude-code@2.1.77
npm install -g @anthropic-ai/claude-code@latest

# 关闭自动更新（临时生效）
export DISABLE_AUTOUPDATER=1

# 永久生效：将上一行写入 ~/.bashrc 或 ~/.zshrc
echo 'export DISABLE_AUTOUPDATER=1' >> ~/.bashrc
```

---

## 🚀 OpenCode 安装指令

常用的 OpenCode 安装与更新命令：

### 安装方式

```bash
# 方式一：curl 一键安装
curl -fsSL https://opencode.ai/install | bash

# 方式二：npm 全局安装
npm i -g opencode-ai
```

### 更新到最新版本

```bash
opencode upgrade
```

---

## 🦞 Openclaw 指令

常用的 OpenClaw 安装、配置及完全卸载指令：

### 安装与初始化

```bash
# 一键安装 OpenClaw CLI（Windows PowerShell 官方安装脚本）
powershell -c "irm https://openclaw.ai/install.ps1 | iex"

# 交互式引导配置（选择模型提供商、设置 API Key、配置 Gateway）
openclaw onboard
```

### 日常配置与运维

```bash
# 将工具配置设为 "full" 模式（启用全部可用工具）
openclaw config set tools.profile "full"

# 重启 Gateway 服务（修改配置后需要重启才能生效）
openclaw gateway restart

# 健康检查并自动修复检测到的问题（配置 + Gateway + 旧版服务）
openclaw doctor --fix

# 打开控制台仪表盘（--no-open 表示不自动在浏览器中打开）
openclaw dashboard --no-open

# 在聊天中输入 /status 可快速查看运行状态诊断信息
/status
```

### 完全卸载

```bash
# 卸载 Gateway 服务 + 本地数据（--all 移除所有组件，--yes 跳过确认提示）
openclaw uninstall --all --yes

# 卸载全局 npm 包（如果通过 npm 安装过）
npm uninstall -g openclaw
```

## 🔌 Chrome DevTools MCP 直连配置

开启 Chrome MCP 直连用户 MCP 的步骤：

1. 在 Chrome 地址栏输入 `chrome://flags/#enable-webmcp-testing`，找到 **WebMCP for testing**，设置为 **Enable**
2. 在 Chrome 地址栏输入 `chrome://inspect/#remote-debugging`，打开"远程调试"开关（toggle）
3. 每次重启 Chrome 后该开关默认保持开启；首次连接某个标签页时会弹出"允许调试"确认框，点击允许即可，之后同一标签页不会再弹出

> chrome-cdp-skill 的守护进程会自动维持连接
> 

### ✨ 新方式：`--autoConnect`（Chrome 144+，推荐）

Chrome 144 起官方支持自动发现并接管你自己正在用的 Chrome，**不再需要** `--remote-debugging-port`、`--user-data-dir`、`--browserUrl` 这一套手动配置。

1. 在正在使用的 Chrome 里打开 `chrome://inspect/`，开启 **Remote Debugging**（远程调试）
2. 在 MCP 配置的 args 里加上 `--autoConnect`
3. Agent 发起连接时 Chrome 会弹权限对话框，点 **Allow** 即可；连接中的标签页会显示调试横幅

```json
{
  "mcpServers": {
    "chrome-devtools": {
      "command": "npx",
      "args": ["chrome-devtools-mcp@latest", "--autoConnect"]
    }
  }
}
```

如果本机 stable 还没到 144，可以指定渠道：

```json
"args": ["chrome-devtools-mcp@latest", "--autoConnect", "--channel=beta"]
```

注意事项：

- `--autoConnect` 默认连接 `--channel` 对应的用户数据目录（默认 stable），多配置文件时连 Chrome 默认 profile，可访问该 profile 下所有窗口
- 必须先手动启动 Chrome，MCP 不会自己拉起浏览器（没开就报“autoConnect is used, but ... isn't running”）
- 仍然可用的旧方式：`--browserUrl http://127.0.0.1:9222`（或 `-u`）、`--wsEndpoint ws://...`；沙箱/远程场景下旧方式更可控
- 好处：用你自己的登录态与扩展，不会另开一个“自动化控制”的干净 profile

## 🌐 Git 代理配置

设置 PowerShell 环境变量，指定 Git Bash 路径：

```powershell
$env:CLAUDE_CODE_GIT_BASH_PATH="E:\\Git\\bin"
```

配置 Git 全局 HTTPS 代理：

```bash
git config --global https.proxy http://127.0.0.1:7890
```

配置 Git 全局 HTTP 代理：

```bash
git config --global http.proxy http://127.0.0.1:7890
```

---

## 🔧 Windows 系统代理设置

从 IE 导入系统代理设置到 WinHTTP：

```bash
netsh winhttp import proxy source=ie
netsh winhttp show proxy
```

重置 WinHTTP 代理设置：

```bash
netsh winhttp reset proxy
```

临时设置cmd窗口走代理

```bash
set http_proxy=http://127.0.0.1:7890
set https_proxy=http://127.0.0.1:7890

set https_proxy=http://127.0.0.1:7897
set https_proxy=http://127.0.0.1:7897
```

---

## 🧩 Claude Code 插件安装

添加和安装 Claude Code 插件：

```bash
/plugin marketplace add anthropics/claude-code
/plugin install frontend-design@claude-code-plugins
```

---

## 🧹 NPM / PNPM 缓存清理

强制清理 npm 缓存（解决安装异常时使用）：

```bash
npm cache clean --force
```

pnpm 缓存管理：

```bash
# 清理 store 缓存（最常用）
pnpm store prune

# 查看 store 信息（路径、大小等）
pnpm store status

# 查看缓存目录路径
pnpm store path
```

---

## 🧩 npx 缓存清理

清理 npx 本地缓存目录（解决 npx 执行异常或版本残留问题）：

```bash
rmdir /s %LOCALAPPDATA%\npm-cache\_npx
```

---

## ⚙️ Claude Code 配置文件示例

以下是 Claude Code 的 JSON 配置文件，包含插件、模型、环境变量等设置：

```json
{
  "effortLevel": "xhigh",
  "enabledPlugins": {
    "chrome-devtools-mcp@chrome-devtools-plugins": true,
    "claude-hud@claude-hud": true,
    "claude-plugins-official": false,
    "superpowers@superpowers-marketplace": true
  },
  "env": {
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1",
    "CLAUDE_CODE_MAX_OUTPUT_TOKENS": "184000",
    "HTTP_PROXY": "http://127.0.0.1:7890",
    "HTTPS_PROXY": "http://127.0.0.1:7890",
    "autoUpdatesChannel": "latest"
  },
  "model": "opus[1m]",
  "skipDangerousModePermissionPrompt": true,
  "hasCompletedOnboarding": true,
  "statusLine": {
    "command": "bash C:/Users/11596/.claude/claude-hud-runner.sh",
    "type": "command"
  }
}
```

---

## 💠 升级 PowerShell 7（解决中文乱码 / 指令经常出错）

Windows 自带的是 Windows PowerShell 5.1，默认编码不是 UTF-8，容易出现**中文乱码、命令解析失败**等问题。升级到 PowerShell 7 后，Claude Code、Codex 等 CLI 都会走新版终端。

### 安装步骤（推荐 MSI）

1. 打开 PowerShell 官方仓库的 Releases 页面：[https://github.com/PowerShell/PowerShell/releases](https://github.com/PowerShell/PowerShell/releases)
2. 下载 **Latest** 版本的 **.msi** 安装包（如 `PowerShell-7.6.4-win-x64.msi`；32 位系统用 `win-x86.msi`）——注意一定要 msi 版，不要 zip
3. 安装第一页勾选项：**除“新建右键菜单（Open here / Run with PowerShell 7 上下文菜单）”外，其余全选**
4. 第二页勾选项：**全选**
5. 安装完成后去设置环境变量，把 PowerShell 7 的路径升到 **Path 列表第一位**，这样 Claude Code、Codex 等都会优先调用 pwsh 7

默认安装路径：

```
C:\Program Files\PowerShell\7\
```

> 环境变量位置：`Win + R` 输入 `sysdm.cpl` → 高级 → 环境变量 → 系统变量 `Path` → 选中 PowerShell 7 路径 → “上移”到最顶部
> 

### 其他安装方式

```powershell
# winget（命令行安装）
winget install --id Microsoft.PowerShell --source winget

# 升级到最新版
winget upgrade --id Microsoft.PowerShell
```

### 验证是否生效

```powershell
# 启动 PowerShell 7（注意命令是 pwsh，不是 powershell）
pwsh

# 查看版本，应为 7.x
$PSVersionTable.PSVersion

# 确认优先调用的是哪个
pwsh
Get-Command pwsh, powershell | Select-Object Name, Source
```

### 彻底解决中文乱码（写入 profile）

```powershell
# 编辑当前用户的 PowerShell 7 配置文件
notepad $PROFILE
```

在打开的文件中加入：

```powershell
# 强制输入输出使用 UTF-8，避免中文乱码
$OutputEncoding = [System.Text.Encoding]::UTF8
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
[Console]::InputEncoding = [System.Text.Encoding]::UTF8
$PSDefaultParameterValues['*:Encoding'] = 'utf8'
```

另外可以在系统层面开启 UTF-8：**设置 → 时间和语言 → 语言和区域 → 管理语言设置 → 更改系统区域设置 → 勾选“Beta：使用 Unicode UTF-8 提供全球语言支持”**（需重启）。这个不建议使用

### 常见坑

- 新版可执行文件叫 `pwsh.exe`，旧版 `powershell.exe` 仍然保留，两者共存；工具里配置终端时要指定 `pwsh`
- 改完环境变量需要**重启终端 / 编辑器**才生效，必要时重启电脑
- 脚本被禁止运行时，以管理员运行：`Set-ExecutionPolicy RemoteSigned -Scope CurrentUser`

---

## 🐧 WSL 安装与常用命令

### 安装 WSL / Ubuntu

```powershell
# 默认安装 Ubuntu，这就是直接安装最新版建议使用这个
wsl --install Ubuntu

# 查看可用发行版
wsl --list --online

# 安装指定版本（如 Ubuntu 24.04 LTS）
wsl --install -d Ubuntu-24.04

# 查看已安装的虚拟系统
wsl -l

# 查看已安装的虚拟系统具体版本是否是wsl2
wsl --list --verbose

# 设置默认实例
wsl --set-default 
```

### 重置 / 重装实例

```powershell
# 1. 注销当前的 Ubuntu 实例（会清空数据，如有重要文件请先备份）
wsl --unregister Ubuntu

# 2. 重新启动 Ubuntu
wsl -d Ubuntu
```

### Windows 与 WSL 路径映射

| Windows 路径 | WSL 内路径 |
| --- | --- |
| `C:\Users\你的名字\Desktop` | `/mnt/c/Users/你的名字/Desktop` |
| `D:\` | `/mnt/d/` |

---

## 🌐 WSL2 代理配置（~/.bashrc）

### 镜像网络模式（Windows 宿主通过 127.0.0.1 访问）

```bash
# WSL2 Proxy（镜像网络模式下 Windows 宿主通过 127.0.0.1 访问）
host_ip=127.0.0.1
export HTTP_PROXY="http://${host_ip}:7890"
export HTTPS_PROXY="http://${host_ip}:7890"
export ALL_PROXY="socks5://${host_ip}:7890"

# 代理开关
proxy_on() {
    host_ip=127.0.0.1
    export HTTP_PROXY="http://${host_ip}:7890"
    export HTTPS_PROXY="http://${host_ip}:7890"
    export ALL_PROXY="socks5://${host_ip}:7890"
    echo "Proxy ON: ${host_ip}:7890"
}

proxy_off() {
    unset HTTP_PROXY HTTPS_PROXY ALL_PROXY
    echo "Proxy OFF"
}
```

### NAT 模式（自动读取宿主 IP）

```bash
# 代理开关（从 /etc/resolv.conf 取宿主 IP）
proxyon() {
    local ip=$(cat /etc/resolv.conf | grep nameserver | awk '{print $2}')
    export http_proxy="http://${ip}:7890"
    export https_proxy="http://${ip}:7890"
    export all_proxy="socks5://${ip}:7890"
    echo "✅ Proxy ON: ${ip}:7890"
}

proxyoff() {
    unset http_proxy https_proxy all_proxy
    echo "❌ Proxy OFF"
}
```

---

## 🛠️ WSL 快捷指令（~/.bashrc）

### 自定义 alias

```bash
# ===== 自定义快捷指令 =====
alias c='claude'
alias cldmax='claude --verbose --dangerously-skip-permissions --effort max'
alias cldr='claude --verbose --dangerously-skip-permissions --effort max --resume'
alias cldxhigh='claude --verbose --dangerously-skip-permissions --effort xhigh'
alias ..='cd ..'
alias ...='cd ../..'
alias update='sudo apt update && sudo apt upgrade -y'
```

> 修改 `~/.bashrc` 后执行 `source ~/.bashrc` 生效。
> 

[CLAUDE精简版.md](CLAUDE%E7%B2%BE%E7%AE%80%E7%89%88.md)